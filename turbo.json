{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"],
      "env": [
        "MARCEL_PROJECTS_AUTH_TOKEN_SECRET",
        "DATABASE_URL",
        "BLOB_STORAGE_URL",
        "BLOB_READ_WRITE_TOKEN"
      ]
    }
  }
}
