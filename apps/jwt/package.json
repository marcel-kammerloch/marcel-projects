{
  "name": "jwt",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "pnpm --filter @repo/auth... build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@repo/auth": "workspace:*"
  },
  "devDependencies": {
    "vite": "^8.1.3"
  }
}
